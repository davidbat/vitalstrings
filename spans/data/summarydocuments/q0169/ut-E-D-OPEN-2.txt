Sparse matrix is defined as: .In the subfield of numerical analysis, a sparse matrix is a matrix populated primarily with zeros (Stoer & Bulirsch 2002, p. 619). The term itself was coined by Harry M. Markowitz. If the majority of elements differ from zero then it is common to refer to the matrix as a dense matrix.
